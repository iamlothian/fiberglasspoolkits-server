import { Model } from '../../model'
import { QueryableRunner, Queryable } from '../../query'

class QueryRunner implements QueryableRunner {

    run<T extends Model>(query: Queryable<T>) {
        
        


    }
    
}